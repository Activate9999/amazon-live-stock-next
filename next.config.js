/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 14 enables the App Router by default. The `experimental.appDir`
  // flag is no longer recognized and causes a build-time warning. Remove it
  // to avoid the Invalid next.config.js options warning on Vercel.
}

module.exports = nextConfig
